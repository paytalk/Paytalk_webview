import React from "react";
import PayComplete from "../components/Store/PayComplete";

const PayCompletePage = () => {
    return (
        <>
            <PayComplete />
        </>
    );
};

export default PayCompletePage;
